#include <stdio.h>
#include <stdlib.h>
#define SUM(x, y) (x + y)

/*
 * main - entry point
 * sum(x, y) - calculates the sum of x + y
 * @x: an int
 * @y: an int
 * Return: (0)
 */

int main(sum x, y)
{
	x = 5;
	y = 4;

	printf("%d\n", SUM);
	return (0);
}
