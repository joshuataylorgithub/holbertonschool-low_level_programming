#include <stdio.h>
#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0.
 */




int main()
{
	int num = -10;
	int absValue = _abs(num);
	printf("The absolute value of %d is %d\n", num, absValue);
	return 0;
}
