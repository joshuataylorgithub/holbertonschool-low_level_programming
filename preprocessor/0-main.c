#include "0-object_like_macro.h"
#include <stdio.h>
/**
 * main - checks the code
 *
 *
 * Return: (0)
 */
int main()
{
	int array[SIZE];

	printf("Size of array: %lu\n", sizeof(array));

	return (0);
}
