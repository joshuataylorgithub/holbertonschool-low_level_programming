#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int base = 0;


	for (base = 0; base <= 9; base++)

{
	putchar("%d", base);
}
	putchar("\n");
	return (0);
}
