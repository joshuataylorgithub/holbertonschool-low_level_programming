#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - Entry point
*
* Return: Always (0)
*/

int main(void)
{
	int i;

	for (i = 'a' ; i <= 'f' ; i++)
{
		putchar(i);
}
	putchar('\n');
	return (0);
}
