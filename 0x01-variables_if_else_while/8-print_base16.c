#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - Entry point
*
* Return: Always (0)
*/

int main(void)
{
	int number;
	int alpha;

	for (number = '0' ; number <= '9' ; number++)
	{
		putchar(number);
	}
	for (alpha = 'a' ; alpha <= 'f' ; i++)
	{
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
