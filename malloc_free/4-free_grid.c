#include "main.h"
/**
 * free_grid - frees the previously allocated grid
 *
 * Return: (nothing)
 */

void free_grid(int **grid, int height);
{
	for (i = 0; i < height; i++)
	{
	free(grid);
	}

	free(grid);
}
