#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * Write a program that multiplies two numbers.
 * Return: (0)
 *
 */

int main()
{
	int x;
	int y;

	for (x = 4; y = 5;)
	{
		printf(x * y);
		return (0);
	}
}
