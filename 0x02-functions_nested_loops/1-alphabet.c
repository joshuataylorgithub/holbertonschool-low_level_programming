#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <main.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 **/

int main(void)
{
	char alpha;

	for (alpha = 'a' ; alpha <= 'z' ; alpha++)
	{
	putchar(alpha);
	}
	putchar('\n');

	return (0);
}
